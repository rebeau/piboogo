'use client';

import { Box } from '@chakra-ui/react';
import CustomEditor from '../CustomEditor';

const Editor = () => {
  return (
    <Box w={'100%'} h={'21rem'}>
      <CustomEditor />
    </Box>
  );
};

export default Editor;
